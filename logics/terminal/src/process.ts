export function terminalProcess() {
	return {
		// sendSignal() {},
		// onSignal(listener: (signal: string) => void) {
		// },
		// changeCursorPosition(x: number, y: number) {
		// },
		// onCurserPositionChanged(listener: (x: number, y: number) => void) {
		// },
		// changeColor() {
		// },
		// onColorChanged() {
		// }
	}
}
